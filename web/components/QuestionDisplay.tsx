import { Flex, Heading } from "@chakra-ui/react";

const QuestionDisplay: React.FC = () => {
  return (
    <Flex marginTop={10} marginBottom={10} justify="center">
      <Heading>How are you doing?</Heading>
    </Flex>
  );
};

export default QuestionDisplay;
